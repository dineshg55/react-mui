# Responsive Profile Layout
- A responsive layout built with React and Material UI (MUI).

## Technologies
- React
- Material UI (MUI) - Box component with sx prop for styling

## Features
- Responsive layout: side-by-side on larger screens, stacks vertically on smaller screens
- Profile image section with bottom text strip
- Right-side sections with equal height distribution
